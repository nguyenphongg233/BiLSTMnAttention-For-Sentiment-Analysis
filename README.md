# BiLSTMnAttention-For-Sentiment-Analysis
Mini Project for IT3160

## Setup

Use Python 3.11 or 3.12, then install the project dependencies:

```bash
python -m pip install -r requirements.txt
```

The notebook/script expects `pandas`, `tensorflow`, and the rest of the ML stack to be installed before running.
